"""W5 fixtures placeholder.

Per the W5 Implementation Readiness Checklist §4.6, the multilingual
golden set ``w5-golden-v1`` (12 entity + 4 todo samples in JSONL) must be
authored once Implementation is authorized. Until then, only this empty
package marker exists.

When the golden JSONL is committed, place it at:
    tests/w5/fixtures/golden/w5_golden_v1.jsonl
The manifest schema validator will require this artifact path; see
``docs/design/W5_EVIDENCE_MANIFEST_SCHEMA_v1.json``.
"""