# PRD — 跨语言实体关联（W5）

> **版本**: v1.1
> **日期**: 2026-09-07
> **状态**: 已裁决（D1-D3 全部落档），进入技术设计阶段
> **父规划**: [ONTOLOGY_SEMANTIC_CONTRACT_PLAN.md](../planning/ONTOLOGY_SEMANTIC_CONTRACT_PLAN.md)（W5 = L4/L5，依赖 W1+W4）
> **承接**: W1+W2 契约已落地（commit 8bf08f6、f576a06）；W3 纠偏回流 + W4 实体规范化已落地（PR #12，v1.0.6）；离线模式 v1.1.0（PR #13）
> **变更说明**: v1.1——D1-D3 三项裁决落档（2026-09-07 用户确认：D1 embedding 先行 / D2 不做翻译 / D3 L5 纳入本期）；v1.0 初版——将 ROADMAP v0.9.5 的 L4/L5 结论细化为可实施需求；目标版本 v1.2.x

---

## 1. 背景与问题

### 1.1 现状

W4 落地后的实体规范化能力：

- `Entity.aliases` JSON 字段已存在（同语言别名，靠解析自学习）
- `synonym_dict.py` 两层字典（JSON 配置 + 代码种子），但**种子全部是同语言等价**（如 "张三"→["Zhang San", "老张"] 中英文混合条目极少量，无系统性跨语言覆盖）
- `EntityResolutionEngine` 7 步 pipeline（exact→alias→synonym→difflib→fuzzy→context→llm），阈值 0.85/0.70，`synonym_match`/`difflib_match` 为 confirm-only
- `Step03_SemanticEmbedding` 已存在，`embedding_provider` 支持 local / api（OpenAI 兼容）——**embedding 基础设施已具备，无需新增向量库**

缺口：

- **跨语言主体归一缺失**："John Smith"（英文纪要）与"约翰·史密斯"（中文录入）会创建两个实体，人脉图谱碎片化，承诺/待办跟进断链
- W1 契约黄金集**仅中文样本**——换英文/日文输入时抽取质量无回归仪表
- LLM 抽取 prompt 未明确要求输出跨语言别名——多语言原文中的译名信息在抽取阶段被丢弃
- 同一待办的跨语言表述（"follow up next week" vs "下周跟进"）无归一路径（ROADMAP L5）

### 1.2 核心问题

W1-W4 解决了"中文单语言的解析显式化、纠偏闭环、确定性规范化"，但用户真实输入是**多语言混合**的（英文会议纪要 + 中文复盘 + 日文名片）。跨语言碎片直接削弱产品的核心承诺——人脉/承诺不遗漏。

---

## 2. 目标与非目标

### 2.1 目标

| # | 工作流 | 目标 | 度量 |
|---|---|---|---|
| G-W5-1 | 抽取多语言 | 英文/日文原文录入时，实体抽取质量与中文同级 | 黄金集新增多语言分层，抽取断言全过 |
| G-W5-2 | 跨语言候选 | 同一主体跨语言表述能产生"可能是同一人"候选（不自动合并） | `"John Smith" 录入后 → 候选列表含"约翰·史密斯"，置信度 ≥ 阈值，active entity 数量不变 |
| G-W5-3 | 纠偏闭环 | 跨语言确认/拒绝走 W3 审计回流 | `EntityCorrection` 行含 `action=select_existing`，候选集留痕 |
| G-W5-4 | 跨语言待办（L5） | 同一待办的中/英/日表述归一到同一 Todo | 时间语义 + LLM 归一化，confirm-only |

### 2.2 非目标（明确不做）

- ❌ **不引入实时翻译 API**（成本高、延迟大——ROADMAP 既定结论）
- ❌ **不做自动翻译录入文本**（保持原文 + 多语言实体，ROADMAP 决策点 3 默认答案）
- ❌ **不做自动合并**——跨语言匹配天然低置信，必须用户确认（继承 W4 硬边界）
- ❌ 不新增向量库（embedding 复用 `Step03` 既有基础设施；若选 embedding 路线，仅复用现有 provider）
- ❌ 不改前端 UI 结构（复用现有多候选人脉纠偏 UI，仅候选依据文案需展示"跨语言匹配"）

---

## 3. 用户故事

| # | 角色 | 故事 | 验收 |
|---|---|---|---|
| US-W5-1 | 用户 | 我先用英文纪要录入了 "John Smith"，后用中文补录"约翰·史密斯"，系统提示"可能是同一人" | 候选列表出现对方，依据含"跨语言匹配"；确认后 aliases 互相追加，仅一个 active 实体 |
| US-W5-2 | 用户 | 我拒绝了跨语言候选，因为其实是两个人 | 拒绝不触发任何合并；`EntityCorrection` 留痕；下次同名输入不再重复推荐（same-pair 冷却） |
| US-W5-3 | 用户 | 我录入日文名片 "ジョン・スミス"，系统能识别并与已有英文/中文实体关联 | 三语映射场景候选产生 |
| US-W5-4 | 用户 | 英文纪要里的 "follow up next week" 和我中文待办"下周跟进"被归为同一待办 | L5 归一候选 confirm-only |
| US-W5-5 | 开发者 | 我改抽取 prompt 或换模型时，多语言抽取质量有回归仪表 | 黄金集多语言层 CI 可跑（LLM 成本 job 手动触发，同 W2） |

---

## 4. 功能需求（FR）

### 4.1 FR-W5-1 抽取阶段多语言输出契约（对应 ROADMAP 结论 2）

扩展 W1 抽取 prompt 契约：当原文为非中文时，LLM 需同时输出：

- 实体**原文写法**（如 "John Smith"）
- **中文规范名**（如 "约翰·史密斯"）——作为 `aliases` 之一写入
- 契约 schema 增加可选字段（`canonical_zh` / aliases 扩展），**向后兼容**（缺省时行为不变）

> 实现原则：抽取阶段一次调用顺带产出跨语言别名，避免解析后二次 LLM 调用。

### 4.2 FR-W5-3 跨语言匹配策略（对应 ROADMAP 结论 3）

`EntityResolutionEngine` 新增 `_step_cross_language`：

- 触发条件：新实体与候选实体 name 分属不同语言（拉丁 vs CJK 简易判别 + 别名表查询）
- 两级匹配：
  1. **字典级**：跨语言同义词字典命中 → confidence 0.97，confirm-only（复用 `_step_synonym` 模式）
  2. **语义级**：路线按 §7 决策点 D1 裁决（embedding 余弦相似度 or LLM prompt 判断），置信度上限 0.90，confirm-only
- **硬约束**：任何跨语言命中 `action=CONFIRM`，绝不 `MERGE`

### 4.3 FR-W5-2 跨语言同义词字典扩展

`synonym_dict.py` 字典结构扩展为三语：

- 种子条目 ≥ 10 组人名 + 5 组公司名（中↔英↔日，全部合成数据，红线 §6-1）
- `find_aliases(name, type_)` 语义不变——跨语言别名收编进同一 family
- 用户确认跨语言候选后，被确认对**写入 aliases + 字典 JSON**（自学习回流，同 W4 机制）

### 4.4 FR-W5-4 纠偏闭环集成（复用 W3）

- 跨语言候选的确认/拒绝走既有 `POST /events/{id}/correct`，`EntityCorrection` 行 `correction_type=entity`，候选集含跨语言候选 id
- same-pair 冷却：同一实体对被用户拒绝后，N 天内（默认 30，可配置）不再重复推荐
- PII 红线与 W3 一致（原文过 redact、日志零原文）

### 4.5 FR-W5-5 黄金集多语言分层（扩展 W2）

- 黄金集新增 `multilingual` 层：英文/日文样本各 ≥ 5 条（**仅合成数据**）
- 断言范围：persons 抽取非空 + 姓名原文保留 + `canonical_zh` 产出
- CI 触发策略与 W2 一致：workflow_dispatch 手动 + 定期（LLM 成本 job 不阻塞普通 PR）

### 4.6 FR-W5-6 跨语言待办关联（L5，对应决策点 D3）

- Todo 匹配新增跨语言归一候选：基于时间语义（`due_date` 相对表述解析已有多语言需求）+ LLM 判断"同一待办"
- confirm-only；拒绝留痕（复用 `correction_type=todo`）

---

## 5. 非功能需求（NFR）

| # | 需求 | 指标 |
|---|---|---|
| NFR-W5-1 | 跨语言匹配不拖慢解析 pipeline | `_step_cross_language` 单事件增量 < 500ms（字典级 O(1)；语义级批量一次调用） |
| NFR-W5-2 | 零 schema 漂移 | 不新增表；`aliases`/`properties` JSON 字段扩展承载 |
| NFR-W5-3 | 零自动合并 | 跨语言命中 100% CONFIRM（测试门禁硬断言 active entity 数量不变） |
| NFR-W5-4 | 多语言抽取无回归 | 黄金集 multilingual 层字段断言全过；中文层分数不下降 |
| NFR-W5-5 | embedding 复用 | 若选 embedding 路线：不新增向量库/新 provider，复用 `embedding_provider` 既有链路 |
| NFR-W5-6 | 跨用户隔离 | 候选产生与字典自学习均强制 `user_id` 作用域；字典 JSON 按用户隔离或全局只读种子 |

---

## 6. 安全区与红线（继承父规划 §5 + W3/W4 增量）

1. **黄金集/字典种子仅合成数据**——真实姓名/公司名一律伪造（三语一致）
2. **零自动合并**——跨语言匹配置信度天然低于同语言，全自动合并风险放大，confirm-only 是硬边界
3. **拒绝冷却可配置**——冷却期写入 `Entity.properties`，禁写死
4. **契约向后兼容**——schema 扩展字段全部 optional，旧数据零迁移
5. **翻译边界**——禁止在解析链路调用外部翻译 API（唯一例外：LLM 抽取时的原生多语言能力）

---

## 7. 决策点裁决记录（2026-09-07 用户确认，全部落档）

| # | 议题 | 选项 | 裁决 |
|---|---|---|---|
| D1 | 语义级跨语言匹配路线（ROADMAP 决策点 2） | A. embedding 余弦相似度（复用 Step03，需对跨语言对效果验证）；B. LLM prompt 判断（每次解析 +1 次批量调用，成本+延迟） | ✅ **A embedding 先行**——复用既有链路零增量成本；黄金集验证不达标再降级 B |
| D2 | 是否支持自动翻译录入文本（ROADMAP 决策点 3） | A. 不翻译，保持原文+多语言实体（既定）；B. 提供可选翻译预览 | ✅ **A 不做翻译**——继承既定结论 |
| D3 | L5 跨语言待办是否纳入本期 | A. 纳入（+0.5 周）；B. 延后至 W5.5 | ✅ **A 纳入本期**——与 L4 共享确认 UI/审计链路，一次性补齐跨语言闭环 |

裁决后边界确认：M-W5.4（跨语言待办）从"若 D3=A"转为**确定交付**；技术设计中 embedding 路线需定义黄金集验证的达标线（跨语言对召回率），不达标时降级 LLM prompt 的触发条件写明。

---

## 8. 里程碑与验收

| 里程碑 | 交付物 | 验收标准 |
|---|---|---|
| **M-W5.1** | FR-W5-1 抽取契约扩展 + FR-W5-5 黄金集多语言层 | 英/日样本抽取断言全过；契约哈希升级按 W1 流程；中文层无回归 |
| **M-W5.2** | FR-W5-2 三语字典 + FR-W5-3 匹配策略 | 三语映射场景候选产生；跨语言命中 100% CONFIRM；active entity 数量不变 |
| **M-W5.3** | FR-W5-4 纠偏闭环 + 冷却 | 确认→aliases 双向追加；拒绝→留痕+30 天冷却；`EntityCorrection` 行完整 |
| **M-W5.4**（若 D3=A） | FR-W5-6 跨语言待办 | 中/英待办归一候选 confirm-only；拒绝留痕 |
| **M-G3** | 真实用户 e2e：`scripts/e2e/e2e_w5_real_user.py` | 英文纪要→中文补录→候选确认→aliases 合一 全链路 PASS + 证据归档 |

排期估算：M 级，约 2 周（D3=A 时 +0.5 周）——与父规划 W5 标注一致。

---

## 9. 生命周期后续

PRD（本文档，待裁决 D1-D3）→ **技术设计文档** → **测试计划**（G1-G4 + e2e）→ 实现 → 验证 → 发布（v1.2.0）。文档先行，达成共识后进代码。

依赖确认：W1（契约）✅、W3（纠偏回流底座）✅、W4（规范化+字典+confirm-only 机制）✅——W5 无前置阻塞。
